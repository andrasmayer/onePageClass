The SPA using class based views.
Every view has 2 must have procedures : init and events.
routing use location.hash .

View file is at ./src/Components/Router/views.js
Standard views folder is at ./src/Views
